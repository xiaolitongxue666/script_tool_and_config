# Project Memory (Compact)

1) **职责与两阶段**：独立工具脚本（`scripts/tools/**`）**永不删除**，与部署无关；部署仅经 chezmoi + `install.sh` / `deploy.sh` / `manage_dotfiles.sh apply`（**须 `--force`**）。Phase 1 本仓库 `eval "$(fnm env)" && ./deploy.sh` → Phase 2 agent-config `bash scripts/install-tools.sh`；各 OS/WSL `$HOME` 独立各跑一遍。
2) **目录分层契约（先分类再写代码）**：**库 = `scripts/lib/**`**（含 `scripts/lib/chezmoi/`）——shebang 保留但**不设 `+x`**、**禁止 `set -euo pipefail`**（污染调用方）、不调 `start_script`/`end_script`、结果走 stdout 而日志**必须走 stderr**；**入口 = `scripts/chezmoi/`、`scripts/deploy_utils/`、`scripts/{linux,windows}/`、`scripts/tools/`**——`#!/usr/bin/env bash` + `set -euo pipefail` + `chmod +x` 必需。判断法：被 `source` → 库；被 `bash <file>` 执行 → 入口。**`scripts/common/` 已取消**（`scripts/common.sh`→`scripts/lib/common.sh`；`scripts/common/deploy_utils/`→`scripts/deploy_utils/`；`scripts/common/*` 工具→`scripts/tools/*`；`scripts/chezmoi/*` 库→`scripts/lib/chezmoi/`；`ffmpeg-magic`→`ffmpeg_magic`）。库幂等：`source` 前用独有函数探针守卫（`declare -F echo_color_message`），否则 `readonly` 重复声明报错。
3) **部署入口职责 + 回归守卫（2026-09 事故）**：`install.sh`（首次安装，`[1/6]`→`[6/6]`）→ `deploy.sh`（增量部署 + 诊断展示）→ `manage_dotfiles.sh`（status/diff/apply/edit），三者共享 `scripts/lib/chezmoi/chezmoi_core.sh`，**职责边界不得混同**。曾发生 `deploy.sh` 被**整份覆盖成 `install.sh` 内容**（同 sha256）的事故：已从 `git show HEAD:deploy.sh` 恢复并只重放路径迁移。`tests/test_contracts.sh` 已加硬断言防复发（`cmp -s install.sh deploy.sh` 必须不同；`install.sh` 含 `[1/6]`；`deploy.sh` 含 `DIAGNOSE_SCRIPT` 且 `install.sh` 不得含）。**改动任一个后必须两个都跑一遍**。
4) **平台脚本命名与排序**：chezmoi 按**目标名（剥 `run_once_`/`.tmpl`）字母序**执行 → `00-` < `90/91/93-` < `install-*` < `linux-/macos-/windows-`；编号前缀**只**能表达"排在 `install-*` 之前/之后"，**不能**表达"N 层之后"。平台专属**脚本**用前缀 `run_once_{linux,macos,windows}-`；**`$PLATFORM` 在 macOS 上是 `darwin` 而前缀是 `macos-`**，任何按前缀做文件名匹配处都须做此归一（`install_helpers.sh` 的 `extract_software_name_from_script()` 已实现）。**重命名/移动 `run_once_*` 不触发重跑**（状态键 = `sha256(渲染后内容)`，与路径无关），改内容才重跑。
5) **`run_on_*` 已彻底取消**（它**不是** chezmoi 平台目录，子目录会被原样部署到 `~/run_on_*/`，曾致 yabai/i3/Ghostty 读不到配置）。平台专属 **dotfile** 一律放**源根** + `.chezmoiignore` **模板**按 `.chezmoi.os` 过滤；`.chezmoiignore` **不支持 `!` 取反**（取反会让该目录脚本一起被忽略、**永不执行**）。同批修复 `~/chezmoi.toml`、`~/detect_windows_git_paths.sh` 泄漏。诊断过滤用 `chezmoi --source .chezmoi ignored`（macOS 返回 7 项：`.config/{alacritty,i3}`、`_bash_profile_{darwin,windows}`、`chezmoi.toml`、`detect_windows_git_paths.sh`、`secure_crt`），**勿硬编码平台文件清单**；`include` 相对**源根**解析（非 `.chezmoitemplates/`）。
6) **chezmoi 源与库拆分**：zsh canonical `.chezmoi/dot_zshrc.tmpl`；映射 `config_mappings.sh`。聚合入口 `common_install.sh` / `chezmoi_core.sh`；子模块 `detect_platform.sh`、`packages.conf`+`software_policies.sh`、`package_install.sh`、`brew_macos_network.sh`、`chezmoi_proxy.sh`、`chezmoi_lock.sh`、`chezmoi_apply.sh`。macOS 专属**脚本**为 `.chezmoi/run_once_macos-*.sh.tmpl`（**无** `scripts/darwin/`）。**本机私有**：`~/.zshrc.local` / `~/.bashrc.local`（模板**最末尾** `source`；Windows Git Bash **仅**用 `.bashrc.local`，无 zsh；Phase 2 dsh 等写 local，**禁止**改正文 `~/.bashrc`）；误入根目录的 `${APPDATA}\npm/` 已 gitignore。
7) **Layer 4 CLI / Agent 体系**：claude / codex / cursor 由 `run_once_90` / `91` / `93`；Phase 2 `install-tools.sh` 也会装全部 Agent CLI（Claude / Cursor / Codex / Pi + CodeGraph）；**两侧当前保留（存量双路径）**。**Pi 仅 agent-config Phase 2**；`93` 无 GUI 时跳过 Cursor。**CodeWhale 已从本仓与 agent-config 移除（勿恢复）**（本仓勿保留 `run_once_92`/`run_once_94`/`dot_pi`/PI 文档副本文；勿恢复 cargo `install-deepseek`）。权威长文 [PROJECT_AGENT_MEMORY.md](PROJECT_AGENT_MEMORY.md)。
8) **Pi / SSH 终端**：Harness 在 agent-config；本仓 **`~/.pi_ssh_helpers.sh`**（`dot_pi_ssh_helpers.sh.tmpl`）为 SSOT，由 `.zprofile`/Linux `.bashrc` source；SSH 会话设 `TERM=xterm-256color`、TUI cleanup、**`pi-reset`**；诊断 `scripts/deploy_utils/diagnose_vps_ssh_pi.sh`。
9) **代理（默认启用）**：唯一入口 `chezmoi_core.sh` → `chezmoi_setup_proxy`；**WSL → 宿主机** `http://<resolv nameserver>:7890`；headless 原生 Linux（VPS）扫描 `PROXY_PROBE_PORTS`（含 **17890**）并注入 override-data；桌面 Linux/macOS/Windows → `127.0.0.1:7890`；禁用 `PROXY=none/false` 或 `NO_PROXY=1`。Pacman/apt 直连国内源；**macOS brew 见 #20**。
10) **WSL 与 Windows 完全独立**：各环境 `$HOME`/fnm/npm/chezmoi 目标分开，各自跑 install。WSL 里 `install.sh` **只装 WSL**，与宿主机 npm **无关**（`:7890` 只是网络出口）。已装判定看**当前环境** `npm prefix -g` / `npm root -g`；`/mnt/c`、`/mnt/host/c`、`C:\`、`AppData/Roaming/npm` 在 WSL 里不算已装。`/mnt/host/wslg/.../fnm_multishells` 是 **WSL 本机 fnm**（勿当 Windows interop）。`_wsl_prepend_npm_global_bin` **始终置顶**本地 prefix/bin。禁止从 WSL 改 Windows npm / 调 `cmd.exe`。规则 `.cursor/rules/wsl-windows-isolation.mdc`。
11) **Windows chezmoi override SSOT**：`chezmoi_build_base_args` + `chezmoi_capture_*` 注入 `windows_git_*`；`deploy.sh`（设 `CHEZMOI_PROJECT_ROOT`）、`manage_dotfiles.sh`、`diagnose_deployment.sh`、`install_helpers.sh` 须走封装；**禁止**裸 `chezmoi status/diff/apply`。Win10 全量安装用 Git Bash **`--noprofile --norc`**（login shell 污染 stdout → override-data 空）。
12) **macOS bash 3.2**：禁止 `declare -A`；`set -u` 下空数组勿 `"${arr[@]}"`；代理禁用用 `case` 勿 `${var,,}`。
13) **验证与 CI**：8 个测试全过 —— `tests/test_contracts.sh`（**结构与命名契约**：目录分层 / 库 vs 入口 / 可执行位 / 文档链接 / 全角标点 / 末尾换行 / LF / snake_case / **部署入口唯一性**，24 断言）+ `test_syntax.sh`（全仓语法 + 全角标点回归）+ `test_proxy.sh` + `test_semver_compare.sh` + `test_software_policies.sh` + `test_install_report_status.sh` + `test_winget_msix_fallback.sh` + `test_bashrc_prompt_guard.sh`；部署后跑 `verify_installation`。**CI `.github/workflows/ci.yml`**（ubuntu+macos）：syntax/contracts/全部单元测试/编码检查**硬性阻断**，shellcheck **软性观测**；CI **只跑只读检查**，不执行 `install.sh` / `chezmoi apply`。WSL 上 applicable linux run_once≈13（另滤 i3wm/alacritty；非 WSL 非 Arch≥15，含 `run_once_install-clangd`）；`test_proxy` 本地默认用例须 mock 非 WSL（真实 WSL 默认宿主机:7890）。
14) **`install.sh` 六步**：`[3/6]` apply → `[4/6]` `ensure_platform_software.sh` 补装+默认升级；Intel 源码编译勿当卡死（20s 心跳）；重型依赖见 #20。`--no-upgrade` / `SKIP_SOFTWARE_UPGRADE=1` 可关；全量 `install.sh`，日常 `deploy.sh`。**实测**：exit 0，`[1/6]`→`[6/6]` 全完成，摘要「通过 5 项, 警告 0 项, 失败 0 项」。**注意** `chezmoi apply -v` 的 diff 行以 `+` 开头，其中的 "ERROR" 字样**不是**运行时错误（勿误计数）。
15) **tmux / OMZ**：Linux/macOS/WSL `dot_tmux.conf.tmpl`；Catppuccin **v2.3.0**；TPM yank/resurrect/continuum。OMZ/插件由 `.chezmoiexternal.toml.tmpl`（linux/darwin）负责；`deploy.sh` **不**内联装 Zsh/OMZ，仅 `check_zsh_omz`。
16) **rmux（仅 Windows）**：v0.5.0；`dot_rmux.conf.tmpl` → `~/.rmux.conf`；**apply 后须 `Prefix+r`**；见 [RMUX_WINDOWS.md](RMUX_WINDOWS.md)。
17) **ensure 策略与补装**：`latest` / `minimum:0.11.0`（Neovim 二进制）/ `pinned:0.5.0`（rmux）/ `skip`；清单 [SOFTWARE_LIST.md](SOFTWARE_LIST.md)。补装须 `chezmoi execute-template --file <绝对路径>`，**禁止**无 `--file`。Neovim **配置**属用户 `~/.config/nvim` 独立仓，本仓不并入。
18) **ensure 升级/补装细节**：uv `self update`（**包管理器装的如 choco/brew 会失败** → 按路径识别输出 INFO、不刷 WARNING）；**fnm `self-update` 已废弃（≥1.36 移除）**，多 OS 路径 brew/winget(`Schniz.fnm`)/pacman → 官方脚本 `bash -s -- --skip-shell`（macOS 跳过，官方脚本默认走 brew）；common-tools 按 `packages.conf`，pkg 为 `-` 的平台项 **跳过**（`upgrade_common_tools_packages` 与 [5/6] 一致，勿报 Failed to install）；Layer4 `ensure_npm_global_latest`（已最新跳过；便携超时默认 180s / `NPM_GLOBAL_INSTALL_TIMEOUT_SECS`；`--no-fund --no-audit`；默认 `npm_config_registry=npmmirror` + `NO_PROXY`，不写 `~/.npmrc`）；代理 `ensure_proxy_for_download`；macOS brew 经 `_brew_macos_prepare_env`。补装后**二次验证** `check_script_software_installed`（模板 exit 0 ≠ 命令可用，如 apt 无 lazyssh），失败计 SKIPPED+WARNING 不误报 SUCCESS。
19) **[5/6] 报告**：`get_software_report_status` 经 **stdout**、函数 **return 0**。别名：`neovim`→`nvim`≥0.11、`windows-terminal`→`wt`、`nerd-fonts`→FiraMono、`93-install-cursor`→`cursor`（含 macOS `Cursor.app` / winget `Anysphere.Cursor`）、`ghostty`→macOS `.app`；common-tools **跳过** `packages.conf` 中 `-` 的平台项；**`configure-homebrew` 是配置步骤不是命令，报告会一直 Missing（可忽略）**；`category_order` 用**数组**（"AI 工具"含空格，字符串分词会被拆开致该分类永不显示）。
20) **macOS Homebrew 网络**：有 7890 时保留代理并切 brew origin 至 GitHub；`HOMEBREW_NO_AUTO_UPDATE=1` + `HOMEBREW_NO_ENV_HINTS=1`；无代理才可用 tuna。**Intel（无 x86_64 bottle）**：叶子 formula 仍 `brew upgrade`；`NO_INSTALLED_DEPENDENTS_CHECK` **拦不住** formula 自己的依赖（`fastfetch`→ImageMagick）；dry-run 依赖段含 imagemagick/llvm 等则 **跳过**；`install_package` brew 须走 `upgrade_brew_package`（裸 `brew install` 会 auto-update 并源码编 rust/imagemagick）；中断后 keg 可能 unlink → `_brew_link_existing_keg`。`Xcode is outdated` 后无输出不是卡死（20s 心跳）。见 [INSTALL_GUIDE.md](INSTALL_GUIDE.md)。
21) **Windows winget / common-tools**：install/upgrade/list 须 `--source winget`（避 msstore/`0x8a15005e`）；`rg`/`delta`（`BurntSushi.ripgrep.MSVC` / `dandavison.delta`）；失败则 GitHub zip → `~/.local/bin`（**仅 Windows**）。**WT `0x80070422`** = `InstallService` Disabled → `upgrade_winget_id` 自动 `winget download` + `Add-AppxPackage`（仅需 AppXSvc；WT 在跑则跳过，`0x80073D02`）。**OMP** 文案含「安装技术」= 旧 EXE vs 新 MSIX → 从 winget MSIX 抽 exe 到 `~/.local/bin`（解压勿走 Git Bash `/tmp`；成功日志用 `"$dest" --version`，勿报 PATH 上的旧 oh-my-posh）。**勿**单凭 exit 43 sideload：中文「找不到可用的升级 / 没有可用的较新」也是 43。bashrc/profile **前置** `~/.local/bin` 与 `AppData/.../oh-my-posh/bin`。
22) **全局配置 / Cursor+clangd**：中文回复 + 代理（WSL 宿主机 7890 / VPS 17890）；slash/commands canonical 在 agent-config；本仓 `.cursor/rules/` = 项目 rules。Cursor+clangd 见 [CURSOR_CLANGD.md](CURSOR_CLANGD.md) + `scripts/tools/cursor_clangd/`；`93` 无 GUI 跳过 Cursor；`install-clangd` 可装二进制；**brew 无 clangd formula**（ensure 已装则跳过 `clangd`/`clang`/`clang-tools-extra` 模糊搜索）；WSL 扩展须 Remote 侧。**Cursor 集成终端（仅 GUI）**：`--init-file` 盖掉 settings `-l`，也会注入 `CURSOR_AGENT=1`+`TERM=dumb`；**不得**用 `CURSOR_AGENT` 跳过 Starship 或挡住 Linux `exec zsh`；dumb/空 TERM → `xterm-256color`（不覆盖 Ghostty/tmux）；Win 须 `unset MSYS2_PS1`。回归 `tests/test_bashrc_prompt_guard.sh`。
23) **`/commit-push` + `/summary-memory`**：`git-smart-commit` / `summary-project-memory`。**记忆文件实际位置为 `docs/PROJECT_MEMORY.md`（紧凑，本文件）与 `docs/PROJECT_AGENT_MEMORY.md`（权威）**；注意 `summary-project-memory` 工具默认按 **repo 根** 解析 `PROJECT_MEMORY.md`，`--apply` 会写到根目录——须写进 `docs/`。备份目录 `.project-memory-backups/`（**保留最近 2 份**）。提交信息用 **Conventional Commits**（`fix(macos): ...`）。
24) **SSH GitHub 密钥 / VPS**：`github.com` / `xiaolitongxue-vps` 按本机**已有文件**写 `IdentityFile`（personal 优先）；缺文件不写，避免 `no such identity`；`identity_file` 仅非 GitHub Host。VPS Host `xiaolitongxue-vps`，**仅用 git push/pull**；dirty 时 `reset --hard` + `clean -fd` → Phase1/2；验收 `diagnose_vps_ssh_pi.sh`。
25) **格式与交互契约（踩坑合集）**：① **macOS UTF-8 locale 变量名**：`$var` 后紧跟中文/全角标点（`【】（）`）**必须**写 `${var}`（BSD libc 把 CJK 首字节当字母 → 变量名错扩 → `set -u` 报 unbound）；glob 检查用 `compgen -G` 勿 `[ -f glob 2>/dev/null ]`；回归已固化到 `tests/test_syntax.sh`（自定义 Unicode 类，勿用 `grep -P`/字节区间，会误报制表符与 emoji）。② **`.gitattributes`**：catch-all `* text=auto eol=lf` **必须在最前**，置尾会盖掉 CRLF 规则致 Windows 脚本被误转 LF；用 `git check-attr` 验证；**勿**再单独设 `core.autocrlf`（会二次转换）。③ **`grep -c ... || echo "0"` 是反模式**（产生 `"0\n0"` 致数值比较崩），统一 `|| true`。④ **输出语言分而治之**：库 / 被管道消费的脚本（`tests/**`）**英文**，面向用户的交互脚本（`install.sh`/`deploy.sh`/`deploy_utils`/平台脚本）**中文可接受**；注释文档一律中文；**日志前缀一律英文** `[INFO]`/`[SUCCESS]`/`[WARNING]`/`[ERROR]` 以便 grep。⑤ 文档指南统一放 `docs/`（`scripts/deploy_utils/` 下 4 份已迁入）；`openspec/` 已删除（CLI 从未安装、零引用）；`AGENTS.md` 等规则面从 2450 行降至 1473 行（−39%）。
