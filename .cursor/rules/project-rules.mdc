---
description: Project structure and cross-platform shell strategy
globs:
---

# script_tool_and_config 项目规则

## 跨平台 Shell 策略

不同平台使用不同的默认 Shell，模板文件按平台分别维护：

- **macOS / Linux / WSL** → `dot_zshrc.tmpl`（zsh 配置）
- **Windows Terminal 层** → `dot_config/windows-terminal/settings.json.tmpl`（窗口/profile/字体，不含 shell 逻辑）
- **Windows Git Bash 层** → `dot_bashrc.tmpl`（windows 分支）+ `run_on_windows/_bash_profile_windows.tmpl`（登录 PATH/代理）
- **Linux bash（备选）** → `dot_bashrc.tmpl`（linux 分支）
- **Windows 多路复用** → rmux（`dot_rmux.conf.tmpl`，手动 `rmux new`/`rmux a`）；Linux/macOS → tmux + TPM

修改配置时 WT 与 Git Bash 分开维护，不要混写提示符或 PATH 到 WT settings。rmux 详见 `docs/RMUX_WINDOWS.md` 与 `.cursor/rules/rmux-windows.mdc`。

## chezmoi 部署（Agent）

- 应用配置用 `./scripts/manage_dotfiles.sh apply` 或 `./deploy.sh`，勿仅 `export CHEZMOI_SOURCE_DIR` 后 `chezmoi apply`
- **`chezmoi apply` 必须含 `--force`**（`chezmoi_run_apply` / `manage_dotfiles.sh` 已自动注入），避免 `.gitconfig` 等外部修改触发交互菜单卡住
- Windows：`diagnose_deployment.sh` 跳过 `apply --dry-run`；apply 前若 lock 失败用 `fix_chezmoi_lock.sh` 或 `taskkill //F //IM chezmoi.exe`
- 勿对 apply 输出使用 `\| head` / `\| rg` 管道（SIGPIPE）
- `scripts/chezmoi/config_mappings.sh` 为审计/force_apply 映射唯一来源（**bash 3.2 平行数组**，禁止 `declare -A`）
- `chezmoi_ensure_user_config`（`chezmoi_core.sh`）维护 `sourceDir` 与 Windows `[interpreters.sh]`
- macOS 默认 bash 3.2：`deploy.sh` 末尾 Zsh/OMZ 检查与 diagnose 须兼容；详见 `docs/PROJECT_AGENT_MEMORY.md`「macOS bash 3.2」

## Windows Terminal / Git Bash 路径（C 或 D 盘）

- Git for Windows 可能在 `C:` 或 `D:`；**禁止**在 `.chezmoidata.toml` 或 WT 模板写死 `C:\Program Files\Git`
- 检测：`.chezmoi/detect_windows_git_paths.sh`；注入：`chezmoi_run_apply` → `--override-data-file`（源内 `chezmoi.toml [data]` 的 Go 模板**不会**自动求值）
- apply 成功后 `chezmoi_sync_windows_terminal_config` 同步到 WT LocalState（override-data 改渲染结果时 `run_onchange` 不一定触发）
- WT 报 `0x80070002`：先 PowerShell `Test-Path` 确认真实 bash 路径，再 `manage_dotfiles.sh apply`，完全重启 WT
- 详见 `docs/PROJECT_AGENT_MEMORY.md`「Windows Terminal：Git Bash 路径 C/D 盘」

## claude-mem 自动检测

Shell 配置模板中内置了 claude-mem 项目级记忆自动检测：

- `claude()` 命令从 `$PWD` 向上查找 `.claude-mem/settings.json`
- 找到 → 使用项目级记忆，设置 `CLAUDE_MEM_DATA_DIR`
- 未找到 → 使用 `~/.claude-mem` 全局记忆
- `claude-global()` → 强制使用全局记忆

各平台模板文件：
- `.chezmoi/dot_zshrc.tmpl` — macOS / Linux / WSL
- `.chezmoi/dot_bashrc.tmpl` — Linux bash + Windows Git Bash
- `.chezmoi/run_on_windows/_bash_profile_windows.tmpl` — Windows 登录 shell

## chezmoi 源目录

所有配置文件模板位于 `.chezmoi/` 目录，使用 chezmoi 管理 dotfiles。

## CodeWhale（Layer 4 AI 工具）

- 安装脚本：`.chezmoi/run_once_92-install-codewhale.sh.tmpl`（**已移除** `install-deepseek`）。
- WSL：`npm install -g codewhale`（fnm 全局）；Windows 互操作 PATH 不算已安装；**禁止**从 WSL 改 Windows npm。
- 部署：`./deploy.sh` 或 `./scripts/manage_dotfiles.sh apply`（勿管道截断 apply 输出）。
- Agent 必读：`docs/PROJECT_AGENT_MEMORY.md`、`docs/PROJECT_MEMORY.md`、`docs/CODEWHALE.md`；细则见 `.cursor/rules/codewhale.mdc`。
