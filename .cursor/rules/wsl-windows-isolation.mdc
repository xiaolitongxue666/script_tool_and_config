---
description: WSL 与 Windows 宿主机环境隔离（多 OS 独立）
alwaysApply: true
---

# WSL / Windows 环境隔离

script_tool_and_config 兼容 Windows Git Bash、WSL、macOS、Linux。**每个环境完全独立**：`$HOME`、fnm、npm global、chezmoi 目标目录都不共享。

在 WSL 里跑 `install.sh` / `deploy.sh` **只安装 WSL 这一套**，与 Windows 宿主机的 npm / CLI **无关**。不要用 Windows 上的 `claude`/`codex` 证明 WSL 已装，也不要从 WSL 改 Windows 包。

## 必须

- WSL 安装/升级只用 **WSL 内** fnm/npm。`/mnt/host/wslg/runtime-dir/fnm_multishells` 是 WSLg 下的 **WSL 本机** fnm，不是 Windows。
- 已装判定看当前环境的 `command -v` / `npm prefix -g` / `npm root -g`。Windows 盘符（`/mnt/c`、`/mnt/d`、`/mnt/host/c`、`C:\`、`AppData/Roaming/npm`）在 WSL 里 **不算已装**。
- 各 OS / 各 WSL distro 分别跑 `install.sh` / `deploy.sh`；Phase 2 在对应环境再跑 agent-config `install-tools.sh`。
- 代理可以指向宿主机 Clash `:7890`（网络出口），这不是软件环境混合。

## 禁止

- 从 WSL 改 Windows npm（`/mnt/c/.../npm`、`/mnt/host/c/.../npm`、`cmd.exe npm`、`npm.cmd`）。
- 从 WSL 调用 `cmd.exe` / `powershell.exe` 装、卸、清 Windows 包。
- 把 `/mnt/host/*`、`/mnt/wslg/*`、`*fnm_multishells*` 一律当成 Windows（会误伤 WSL 本机 fnm，[4/6] 会 skip claude/codex）。
- 用 Windows 上的 `claude`/`codex` 证明 WSL Layer 4 已装或跳过 WSL 安装。

判定实现：`_is_windows_interop_path`（[`scripts/chezmoi/package_install.sh`](../../scripts/chezmoi/package_install.sh)）。
