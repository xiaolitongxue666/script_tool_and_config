---
description: CodeWhale install, state paths, and WSL migration notes for agents
globs:
  - .chezmoi/run_once_92-install-codewhale.sh.tmpl
  - docs/CODEWHALE.md
  - docs/PROJECT_AGENT_MEMORY.md
  - scripts/chezmoi/install_helpers.sh
---

# CodeWhale（本项目）

- **已替代** DeepSeek-TUI / `run_once_92-install-deepseek`；安装脚本：`.chezmoi/run_once_92-install-codewhale.sh.tmpl`。
- **安装**：全平台含 WSL 仅用 `npm install -g codewhale`（前置 fnm/node）；**禁止** run_once 内 cargo/brew/winget。
- **WSL**：须 WSL 内 `npm root -g/codewhale` 存在；`/mnt/.../AppData/Roaming/npm` 的 Windows shim **不算**已安装。
- **WSL 禁止**：从 WSL 调用 `cmd.exe` / Windows npm 卸载或修改 Windows 包（含 legacy `deepseek`）。
- **部署**：`./deploy.sh` 或 `./scripts/manage_dotfiles.sh apply`；**勿** `apply | rg | head`（SIGPIPE）。
- **网络**：入口脚本调用 `chezmoi_setup_proxy`（`chezmoi_core.sh`）；WSL 为宿主机 `:7890`，其余 `127.0.0.1:7890`；禁用 `PROXY=none` / `NO_PROXY=1`。
- **状态**：默认 `~/.codewhale/`；`~/.deepseek/` 只读回退；run_once 非破坏性迁移，不删 legacy。
- **勿写** `codewhale setup --migrate`（上游 v0.8.47 无此参数）。

详细问题与排错见 [docs/PROJECT_AGENT_MEMORY.md](../../docs/PROJECT_AGENT_MEMORY.md) 与 [docs/CODEWHALE.md](../../docs/CODEWHALE.md).
