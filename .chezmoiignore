# 忽略不需要管理的文件

# 脚本目录
scripts/

# 原始 dotfiles 目录（已迁移到 .chezmoi）
dotfiles/

# Git 相关
.git/
.gitignore
.gitmodules
.gitattributes

# 编辑器配置
.vscode/
.claude/
.editorconfig

# 文档和配置文件
README.md
LICENSE
settings.json
settings.local.json

# 安装脚本（根目录的 install.sh 保留，但 .chezmoi 中的脚本会被管理）
# install.sh

# 临时文件和构建产物
*.tmp
*.log
*.swp
*.swo
*~

# 系统文件
.DS_Store
Thumbs.db
desktop.ini

# SSH 私钥和公钥（确保不会被纳入 chezmoi 管理）
# 注意：~/.ssh/config 会被管理，但私钥和公钥不会被管理
.ssh/id_*
.ssh/*_rsa
.ssh/*_ed25519
.ssh/*_ecdsa
.ssh/*_dsa
.ssh/*.pub
.ssh/known_hosts
.ssh/authorized_keys
