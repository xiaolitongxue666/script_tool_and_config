# Starship 提示符配置
# 参考: https://starship.rs/config/
# 文档: https://starship.rs/config/

# ============================================
# 全局配置
# ============================================

# 命令超时时间（毫秒）
# 如果命令执行时间超过此值，将不会显示该模块
command_timeout = 10000

# 添加提示符前的空行
add_newline = false

# ============================================
# 提示符格式
# ============================================
# 使用分隔符和背景色创建美观的提示符
# 适配 Dracula 主题颜色方案
format = """
[ ](fg:#bd93f9)\
$username\
[ ](bg:#44475a fg:#bd93f9)\
$directory\
[ ](fg:#44475a bg:#6272a4)\
$git_branch\
$git_status\
[ ](fg:#6272a4 bg:#8be9fd)\
$c\
$elixir\
$elm\
$golang\
$gradle\
$haskell\
$java\
$julia\
$nodejs\
$nim\
$python\
$rust\
$scala\
[ ](fg:#8be9fd bg:#50fa7b)\
$docker_context\
[ ](fg:#50fa7b bg:#ffb86c)\
$time\
[ ](fg:#ffb86c)\
$character\
"""

# ============================================
# 用户名模块
# ============================================
[username]
# 是否始终显示用户名（即使与当前用户相同）
show_always = true
# 普通用户样式（Dracula 紫色背景）
style_user = "bg:#bd93f9 fg:#282a36"
# root 用户样式（红色背景）
style_root = "bg:#ff5555 fg:#282a36"
# 格式
format = "[$user ]($style)"
disabled = false

# ============================================
# 操作系统模块（替代用户名）
# ============================================
[os]
style = "bg:#bd93f9 fg:#282a36"
disabled = true  # 默认禁用，使用用户名模块

# ============================================
# 目录模块
# ============================================
[directory]
# 样式（Dracula 灰色背景）
style = "bg:#44475a fg:#f8f8f2"
# 格式
format = "[ $path ]($style)"
# 路径截断长度（显示最后 N 个目录）
truncation_length = 3
# 截断符号
truncation_symbol = "…/"

# 目录路径替换（使用图标简化常见目录名）
# 注意：顺序很重要，更具体的路径应该放在前面
[directory.substitutions]
"Documents" = "📄 "
"Downloads" = "⬇ "
"Music" = "🎵 "
"Pictures" = "🖼 "
"Desktop" = "🖥 "
"Code" = "💻 "
"Projects" = "📁 "

# ============================================
# Git 分支模块
# ============================================
[git_branch]
# Git 分支符号
symbol = ""
# 样式（Dracula 蓝灰色背景）
style = "bg:#6272a4 fg:#f8f8f2"
# 格式
format = "[$symbol$branch]($style)"
disabled = false

# ============================================
# Git 状态模块
# ============================================
[git_status]
# 样式（与 git_branch 相同）
style = "bg:#6272a4 fg:#f8f8f2"
# 格式
format = "[$all_status$ahead_behind ]($style)"
disabled = false

# ============================================
# 编程语言版本模块
# ============================================

# C/C++ 模块
[c]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Elixir 模块
[elixir]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Elm 模块
[elm]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Go 模块
[golang]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Gradle 模块
[gradle]
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Haskell 模块
[haskell]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Java 模块
[java]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Julia 模块
[julia]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Node.js 模块
[nodejs]
symbol = ""
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Nim 模块
[nim]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Python 模块
[python]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Rust 模块
[rust]
symbol = ""
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# Scala 模块
[scala]
symbol = " "
style = "bg:#8be9fd fg:#282a36"
format = "[ $symbol($version) ]($style)"
disabled = false

# ============================================
# 其他工具模块
# ============================================

# Docker 上下文模块
[docker_context]
symbol = "🐳 "
style = "bg:#50fa7b fg:#282a36"
format = "[ $symbol$context ]($style)"
disabled = false

# 时间模块
[time]
# 是否禁用（默认禁用，需要时启用）
disabled = true
# 时间格式（24小时制，时:分）
time_format = "%R"
# 样式（Dracula 橙色背景）
style = "bg:#ffb86c fg:#282a36"
# 格式
format = "[ ♥ $time ]($style)"

# ============================================
# 字符模块（提示符符号）
# ============================================
[character]
# 成功命令后的提示符（Dracula 橙色）
success_symbol = "[ ](fg:#ffb86c)"
# 失败命令后的提示符（Dracula 红色）
error_symbol = "[ ](fg:#ff5555)"
# Vim 命令模式提示符
vimcmd_symbol = "[ ](fg:#ffb86c)"
